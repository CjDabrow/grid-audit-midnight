/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.env*
!.env.example
.DS_Store
.vercel

# Midnight: compact-generated artifacts (regenerated by `npm run compile:contract`).
# Committed so Vercel can build + serve them: the contract module + browser zk assets
# (public/zk). Ignored: the large Node-provider prover key (CLI-only) and the CLI copy.
/src/contract/managed/registry/keys
/deploy-kit/managed
/deploy-kit/node_modules
# local indexer data
/localnet/.data
